{
  "name": "upi-verifier",
  "version": "1.0.0",
  "description": "PSP-free UPI payment verification service (SMS/email ingest → order matching). Azure Functions v4 + Cosmos, in-memory fallback.",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "test": "node --test"
  },
  "dependencies": {
    "@azure/cosmos": "^4.2.0",
    "@azure/functions": "^4.5.0"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.0.6280"
  }
}
